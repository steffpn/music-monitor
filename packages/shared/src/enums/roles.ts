export enum UserRole {
  ADMIN = "ADMIN",
  ARTIST = "ARTIST",
  LABEL = "LABEL",
  STATION = "STATION",
}

export enum InvitationStatus {
  PENDING = "PENDING",
  REDEEMED = "REDEEMED",
  EXPIRED = "EXPIRED",
  REVOKED = "REVOKED",
}
