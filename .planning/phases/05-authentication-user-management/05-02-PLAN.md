---
phase: 05-authentication-user-management
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/api/src/routes/v1/auth/index.ts
  - apps/api/src/routes/v1/auth/schema.ts
  - apps/api/src/routes/v1/auth/handlers.ts
  - apps/api/src/routes/v1/admin/invitations/index.ts
  - apps/api/src/routes/v1/admin/invitations/schema.ts
  - apps/api/src/routes/v1/admin/invitations/handlers.ts
  - apps/api/src/routes/v1/admin/users/index.ts
  - apps/api/src/routes/v1/admin/users/schema.ts
  - apps/api/src/routes/v1/admin/users/handlers.ts
  - apps/api/src/routes/v1/index.ts
  - apps/api/src/routes/v1/stations/index.ts
  - apps/api/src/routes/v1/airplay-events/index.ts
  - apps/api/tests/routes/auth.test.ts
  - apps/api/tests/routes/admin-invitations.test.ts
  - apps/api/tests/routes/admin-users.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - USER-01
  - USER-02
  - USER-03

must_haves:
  truths:
    - "User can register by redeeming a valid invitation code with email, password, and name"
    - "User can log in with email and password and receive access + refresh tokens"
    - "User can refresh an expired access token using a valid refresh token"
    - "User can log out and their refresh token is revoked"
    - "Admin can create invitation codes with role, scopeId, and maxUses"
    - "Admin can list all invitations"
    - "Admin can revoke an invitation"
    - "Admin can list all users with their roles and scopes"
    - "Admin can deactivate a user (revokes all refresh tokens)"
    - "Admin can reactivate a deactivated user"
    - "Admin can edit a user's role and scope assignments"
    - "Station CRUD endpoints require admin JWT auth"
    - "Snippet URL endpoint requires JWT auth"
    - "ACRCloud webhook still works with shared secret auth (NOT JWT)"
  artifacts:
    - path: "apps/api/src/routes/v1/auth/handlers.ts"
      provides: "Register, login, refresh, logout handlers"
      exports: ["register", "login", "refresh", "logout"]
    - path: "apps/api/src/routes/v1/admin/invitations/handlers.ts"
      provides: "Invitation CRUD handlers"
      exports: ["createInvitation", "listInvitations", "revokeInvitation"]
    - path: "apps/api/src/routes/v1/admin/users/handlers.ts"
      provides: "User management handlers"
      exports: ["listUsers", "deactivateUser", "reactivateUser", "updateUserRole", "updateUserScopes"]
    - path: "apps/api/tests/routes/auth.test.ts"
      provides: "Auth route integration tests"
      min_lines: 100
    - path: "apps/api/tests/routes/admin-invitations.test.ts"
      provides: "Admin invitation route integration tests"
      min_lines: 50
    - path: "apps/api/tests/routes/admin-users.test.ts"
      provides: "Admin user management route integration tests"
      min_lines: 50
  key_links:
    - from: "apps/api/src/routes/v1/auth/handlers.ts"
      to: "apps/api/src/lib/auth.ts"
      via: "hashPassword, verifyPassword, generateTokenPair"
      pattern: "import.*from.*lib/auth"
    - from: "apps/api/src/routes/v1/admin/invitations/handlers.ts"
      to: "apps/api/src/lib/auth.ts"
      via: "generateInviteCode for code generation"
      pattern: "generateInviteCode"
    - from: "apps/api/src/routes/v1/stations/index.ts"
      to: "apps/api/src/middleware/authenticate.ts"
      via: "preHandler: [authenticate, requireRole('ADMIN')]"
      pattern: "authenticate.*requireRole"
    - from: "apps/api/src/routes/v1/airplay-events/index.ts"
      to: "apps/api/src/middleware/authenticate.ts"
      via: "preHandler: [authenticate]"
      pattern: "authenticate"
---

<objective>
Create all auth and admin API routes, then protect existing endpoints with JWT auth middleware. This completes the full backend auth system: users can register, login, refresh tokens, and logout; admins can manage invitations and users; all existing endpoints (stations, snippet URL) require JWT auth while the ACRCloud webhook retains shared secret auth.

Purpose: These routes are the API surface that the iOS app (Phase 6) and admin dashboard (Plan 03) consume. Without them, there's no way to authenticate or manage users.
Output: Complete auth API, admin API, all existing routes protected, full integration test suite.
</objective>

<execution_context>
@/Users/stefanpanaite/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefanpanaite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-authentication-user-management/05-CONTEXT.md
@.planning/phases/05-authentication-user-management/05-RESEARCH.md
@.planning/phases/05-authentication-user-management/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be complete before this plan runs) -->

From apps/api/src/lib/auth.ts:
```typescript
export function hashPassword(password: string): Promise<string>;
export function verifyPassword(password: string, hash: string): Promise<boolean>;
export function generateInviteCode(): string;
export function generateTokenPair(fastify: FastifyInstance, userId: number): Promise<{ accessToken: string; refreshToken: string }>;
export function bootstrapAdmin(): Promise<void>;
```

From apps/api/src/middleware/authenticate.ts:
```typescript
declare module "fastify" {
  interface FastifyRequest {
    currentUser: {
      id: number;
      email: string;
      role: string;
      isActive: boolean;
      scopes: Array<{ entityType: string; entityId: number }>;
    };
  }
}
export function authenticate(request: FastifyRequest, reply: FastifyReply): Promise<void>;
```

From apps/api/src/middleware/authorize.ts:
```typescript
export function requireRole(...roles: string[]): (request: FastifyRequest, reply: FastifyReply) => Promise<void>;
```

From packages/shared/src/constants/index.ts:
```typescript
export const JWT_ACCESS_EXPIRY = "1h";
export const JWT_REFRESH_EXPIRY = "30d";
export const INVITE_CODE_EXPIRY_DAYS = 7;
```

From packages/shared/src/enums/roles.ts:
```typescript
export enum UserRole { ADMIN, ARTIST, LABEL, STATION }
export enum InvitationStatus { PENDING, REDEEMED, EXPIRED, REVOKED }
```

Existing route patterns (from apps/api/src/routes/v1/stations/index.ts):
```typescript
// Plugin-based route registration with TypeBox schema validation
const stationRoutes: FastifyPluginAsync = async (fastify) => {
  fastify.post("/", { schema: { body: StationCreateSchema } }, createStation);
  fastify.get("/", listStations);
  // etc.
};
export default stationRoutes;
```

Existing route registration (apps/api/src/routes/v1/index.ts):
```typescript
const v1Routes: FastifyPluginAsync = async (fastify) => {
  fastify.register(import("./stations/index.js"), { prefix: "/stations" });
  fastify.register(import("./webhooks/acrcloud/index.js"), { prefix: "/webhooks/acrcloud" });
  fastify.register(import("./airplay-events/index.js"), { prefix: "/airplay-events" });
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth routes (register, login, refresh, logout)</name>
  <files>
    apps/api/src/routes/v1/auth/index.ts,
    apps/api/src/routes/v1/auth/schema.ts,
    apps/api/src/routes/v1/auth/handlers.ts,
    apps/api/src/routes/v1/index.ts,
    apps/api/tests/routes/auth.test.ts
  </files>
  <behavior>
    - POST /api/v1/auth/register with valid invitation code + email + password + name returns 201 with { user, accessToken, refreshToken }
    - POST /api/v1/auth/register with invalid/expired/exhausted code returns 400
    - POST /api/v1/auth/register with duplicate email returns 409
    - POST /api/v1/auth/register increments invitation usedCount, sets status to REDEEMED when maxUses reached
    - POST /api/v1/auth/register creates UserScope entry from invitation's scopeId if present
    - POST /api/v1/auth/login with valid credentials returns 200 with { user, accessToken, refreshToken }
    - POST /api/v1/auth/login with wrong password returns 401
    - POST /api/v1/auth/login with non-existent email returns 401
    - POST /api/v1/auth/login with deactivated user returns 401
    - POST /api/v1/auth/login updates lastLoginAt timestamp
    - POST /api/v1/auth/refresh with valid refresh token returns 200 with new { accessToken, refreshToken }
    - POST /api/v1/auth/refresh rotates refresh token (old one revoked, new one created)
    - POST /api/v1/auth/refresh with revoked/expired refresh token returns 401
    - POST /api/v1/auth/logout with valid auth revokes the provided refresh token, returns 200
  </behavior>
  <action>
1. Create `apps/api/src/routes/v1/auth/schema.ts`:
   - `RegisterSchema`: body with code (string, minLength 14, maxLength 14), email (string, format email), password (string, minLength 8), name (string, minLength 1, maxLength 100). Use TypeBox Type.Object.
   - `LoginSchema`: body with email (string, format email), password (string, minLength 1).
   - `RefreshSchema`: body with refreshToken (string, minLength 1).
   - `LogoutSchema`: body with refreshToken (string, minLength 1).

2. Create `apps/api/src/routes/v1/auth/handlers.ts`:
   - `register`: Validate invitation code (findUnique by code, check status=PENDING, not expired, usedCount < maxUses). Check email uniqueness. Hash password with hashPassword(). Use prisma.$transaction to: create user with role from invitation, create UserScope if invitation.scopeId is set (entityType = invitation.role, entityId = invitation.scopeId), increment usedCount and set status to REDEEMED if usedCount+1 >= maxUses. Generate token pair. Return 201 with user (id, email, name, role) + tokens.
   - `login`: Find user by email. If not found or !isActive, return 401 "Invalid credentials". Verify password with verifyPassword(). If wrong, return 401. Update lastLoginAt. Generate token pair. Return 200 with user + tokens.
   - `refresh`: Find refresh token in DB by token value. Check not revoked (revokedAt is null), not expired, user is active. Revoke old token (set revokedAt=now). Generate new token pair. Return 200 with new tokens. If invalid, return 401 "Invalid refresh token".
   - `logout`: Extract current user from request (requires authenticate preHandler). Find refresh token by token value AND userId matching current user. Revoke it (set revokedAt=now). Return 200 { message: "Logged out" }.

3. Create `apps/api/src/routes/v1/auth/index.ts`:
   - Fastify plugin with: POST /register (schema: RegisterSchema), POST /login (schema: LoginSchema), POST /refresh (schema: RefreshSchema), POST /logout (preHandler: [authenticate], schema: LogoutSchema).
   - NOTE: register, login, refresh are PUBLIC (no auth). logout requires auth.

4. Update `apps/api/src/routes/v1/index.ts`:
   - Add: `fastify.register(import("./auth/index.js"), { prefix: "/auth" })`

5. Write `apps/api/tests/routes/auth.test.ts`:
   - Test helper: create invitation directly in DB for test setup.
   - Test register: valid code returns 201 + tokens, invalid code returns 400, expired code returns 400, email conflict returns 409, multi-use increments usedCount.
   - Test login: valid creds return 200 + tokens, wrong password returns 401, unknown email returns 401, deactivated user returns 401.
   - Test refresh: valid refresh returns new tokens + rotates (old revoked), revoked refresh returns 401, expired refresh returns 401.
   - Test logout: with auth header + refresh token, revokes that token, returns 200.
  </action>
  <verify>
    <automated>cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/api && pnpm vitest run tests/routes/auth.test.ts</automated>
  </verify>
  <done>
    - All four auth endpoints work correctly (register, login, refresh, logout)
    - Invitation code validation (status, expiry, maxUses) enforced
    - Token rotation works on refresh
    - All auth tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin routes (invitations + users) and protect existing endpoints</name>
  <files>
    apps/api/src/routes/v1/admin/invitations/index.ts,
    apps/api/src/routes/v1/admin/invitations/schema.ts,
    apps/api/src/routes/v1/admin/invitations/handlers.ts,
    apps/api/src/routes/v1/admin/users/index.ts,
    apps/api/src/routes/v1/admin/users/schema.ts,
    apps/api/src/routes/v1/admin/users/handlers.ts,
    apps/api/src/routes/v1/index.ts,
    apps/api/src/routes/v1/stations/index.ts,
    apps/api/src/routes/v1/airplay-events/index.ts,
    apps/api/tests/routes/admin-invitations.test.ts,
    apps/api/tests/routes/admin-users.test.ts
  </files>
  <behavior>
    - POST /api/v1/admin/invitations requires ADMIN role, creates invitation with code, role, scopeId, maxUses, 7-day expiresAt
    - GET /api/v1/admin/invitations requires ADMIN role, lists all invitations
    - PATCH /api/v1/admin/invitations/:id/revoke requires ADMIN role, sets status to REVOKED
    - GET /api/v1/admin/users requires ADMIN role, lists all users with scopes
    - PATCH /api/v1/admin/users/:id/deactivate sets isActive=false and revokes all user's refresh tokens
    - PATCH /api/v1/admin/users/:id/reactivate sets isActive=true
    - PATCH /api/v1/admin/users/:id/role updates user's role
    - PUT /api/v1/admin/users/:id/scopes replaces user's scope entries
    - Non-admin user gets 403 on all admin endpoints
    - Station CRUD endpoints (POST/GET/PATCH/DELETE /api/v1/stations) require authenticate + requireRole("ADMIN")
    - Snippet URL endpoint (GET /api/v1/airplay-events/:id/snippet) requires authenticate
    - ACRCloud webhook (POST /api/v1/webhooks/acrcloud) still works WITHOUT JWT (shared secret only)
  </behavior>
  <action>
1. Create admin invitation routes (`apps/api/src/routes/v1/admin/invitations/`):
   - `schema.ts`: CreateInvitationSchema with body { role (enum: ADMIN/ARTIST/LABEL/STATION), scopeId (optional int), maxUses (optional int, default 1, min 1) }. InvitationParamsSchema with params { id (int) }.
   - `handlers.ts`:
     - `createInvitation`: Generate code with generateInviteCode(). Create invitation in DB with code, role, scopeId, maxUses, expiresAt (now + 7 days), createdById from request.currentUser.id, status PENDING. Return 201 with invitation (include code prominently -- admin copies this).
     - `listInvitations`: Return all invitations ordered by createdAt desc.
     - `revokeInvitation`: Update invitation status to REVOKED. Return 200.
   - `index.ts`: Register POST /, GET /, PATCH /:id/revoke. All routes have preHandler: [authenticate, requireRole("ADMIN")].

2. Create admin user routes (`apps/api/src/routes/v1/admin/users/`):
   - `schema.ts`: UserParamsSchema { id: int }. UpdateRoleSchema { role: enum string }. UpdateScopesSchema { scopes: array of { entityType: string, entityId: int } }.
   - `handlers.ts`:
     - `listUsers`: Find all users with include: { scopes: true }. Return users with role and scope info.
     - `deactivateUser`: Set isActive=false. Revoke ALL user's refresh tokens (update many where userId and revokedAt is null, set revokedAt=now). Return 200.
     - `reactivateUser`: Set isActive=true. Return 200.
     - `updateUserRole`: Update user's role field. Return 200 with updated user.
     - `updateUserScopes`: Delete all existing UserScope for user. Create new entries from request body scopes array. Use transaction. Return 200 with updated scopes.
   - `index.ts`: Register GET /, PATCH /:id/deactivate, PATCH /:id/reactivate, PATCH /:id/role, PUT /:id/scopes. All routes have preHandler: [authenticate, requireRole("ADMIN")].

3. Register admin routes in `apps/api/src/routes/v1/index.ts`:
   - Add: `fastify.register(import("./admin/invitations/index.js"), { prefix: "/admin/invitations" })`
   - Add: `fastify.register(import("./admin/users/index.js"), { prefix: "/admin/users" })`

4. Protect existing station routes (`apps/api/src/routes/v1/stations/index.ts`):
   - Import authenticate and requireRole from middleware.
   - Add `preHandler: [authenticate, requireRole("ADMIN")]` to every station route (POST /, POST /bulk, GET /, GET /:id, PATCH /:id, DELETE /:id). Can use `fastify.addHook("preHandler", authenticate)` and `fastify.addHook("preHandler", requireRole("ADMIN"))` at plugin level for all routes in the plugin.

5. Protect existing airplay-events routes (`apps/api/src/routes/v1/airplay-events/index.ts`):
   - Import authenticate from middleware.
   - Add `preHandler: [authenticate]` to GET /:id/snippet. Any authenticated user can access snippets (scope filtering on snippet access can be added later if needed).

6. IMPORTANT: Do NOT add JWT auth to webhook routes. The ACRCloud webhook at /api/v1/webhooks/acrcloud must continue using its existing shared secret authentication. Verify by checking the webhook route file is NOT modified.

7. Write tests:
   - `apps/api/tests/routes/admin-invitations.test.ts`: Create admin user + get tokens in beforeAll helper. Test create invitation returns 201 with code. Test list returns invitations. Test revoke sets status. Test non-admin gets 403. Test unauthenticated gets 401.
   - `apps/api/tests/routes/admin-users.test.ts`: Test list users returns users with scopes. Test deactivate sets isActive=false and revokes tokens. Test reactivate sets isActive=true. Test update role. Test update scopes replaces entries. Test non-admin gets 403.
   - Update existing station tests in `apps/api/tests/routes/stations.test.ts`: Add auth headers to all requests (create admin user + get JWT in beforeAll), or note that existing tests will need auth tokens (executor should add a test helper that creates an admin user and returns a valid JWT for test injection).

8. Create a test helper (e.g., `apps/api/tests/helpers/auth.ts`) that:
   - Creates an admin user with hashed password directly in DB
   - Calls POST /api/v1/auth/login to get tokens
   - Returns { accessToken, refreshToken, user } for use in test suites
   - Export this helper for all test files that need authenticated requests
  </action>
  <verify>
    <automated>cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/api && pnpm vitest run tests/routes/auth.test.ts tests/routes/admin-invitations.test.ts tests/routes/admin-users.test.ts</automated>
  </verify>
  <done>
    - Auth routes work: register, login, refresh, logout with correct status codes and validation
    - Admin invitation routes work: create (with 7-day expiry), list, revoke -- all admin-only
    - Admin user routes work: list (with scopes), deactivate (revokes tokens), reactivate, update role, update scopes -- all admin-only
    - Station routes require admin JWT auth (401 without token, 403 for non-admin)
    - Snippet URL route requires JWT auth (401 without token)
    - ACRCloud webhook still works with shared secret auth (not broken by JWT additions)
    - All integration tests pass
  </done>
</task>

</tasks>

<verification>
- `cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/api && pnpm vitest run tests/routes/auth.test.ts tests/routes/admin-invitations.test.ts tests/routes/admin-users.test.ts` -- all pass
- `cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/api && pnpm vitest run tests/routes/stations.test.ts` -- still passes (with auth headers)
- `cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/api && pnpm vitest run tests/routes/webhooks-acrcloud.test.ts` -- still passes (no JWT needed)
- `cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic && pnpm build` -- compiles cleanly
</verification>

<success_criteria>
- Full auth flow works: admin creates invitation -> user registers -> user logs in -> user refreshes token -> user logs out
- Admin can manage invitations (create, list, revoke) and users (list, deactivate, reactivate, edit role, edit scopes)
- Existing endpoints protected with JWT (stations require admin, snippets require any auth)
- Webhook endpoint unaffected by auth changes
- All tests pass including previously-passing tests (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/05-authentication-user-management/05-02-SUMMARY.md`
</output>
