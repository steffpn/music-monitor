---
phase: 01-project-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj/project.pbxproj
  - apps/ios/myFuckingMusic/App/myFuckingMusicApp.swift
  - apps/ios/myFuckingMusic/App/ContentView.swift
  - apps/ios/myFuckingMusic/Models/Station.swift
  - apps/ios/myFuckingMusic/Models/Detection.swift
  - apps/ios/myFuckingMusic/Models/User.swift
  - apps/ios/myFuckingMusic/Models/AudioSnippet.swift
  - apps/ios/myFuckingMusic/Models/Invitation.swift
  - apps/ios/myFuckingMusic/Services/APIClient.swift
  - apps/ios/myFuckingMusic/Services/APIEndpoint.swift
  - apps/ios/myFuckingMusic/ViewModels/.gitkeep
  - apps/ios/myFuckingMusic/Views/.gitkeep
  - apps/ios/myFuckingMusic/Utilities/.gitkeep
  - apps/ios/myFuckingMusic/Resources/.gitkeep
autonomous: false
requirements:
  - DETC-05
must_haves:
  truths:
    - "Xcode project exists and opens without errors"
    - "iOS app builds for simulator targeting iOS 17"
    - "Swift data models mirror the backend shared types"
    - "API client skeleton uses async/await with URLSession"
  artifacts:
    - path: "apps/ios/myFuckingMusic/App/myFuckingMusicApp.swift"
      provides: "SwiftUI app entry point"
      contains: "@main"
    - path: "apps/ios/myFuckingMusic/Services/APIClient.swift"
      provides: "URLSession-based API client with async/await"
      contains: "URLSession"
    - path: "apps/ios/myFuckingMusic/Models/Detection.swift"
      provides: "Detection data model matching backend types"
      contains: "struct Detection"
  key_links:
    - from: "apps/ios/myFuckingMusic/Services/APIClient.swift"
      to: "apps/ios/myFuckingMusic/Services/APIEndpoint.swift"
      via: "endpoint enum for URL construction"
      pattern: "APIEndpoint"
    - from: "apps/ios/myFuckingMusic/Models/Detection.swift"
      to: "packages/shared/src/types/detection.ts"
      via: "conceptual mirror (same field names and types)"
      pattern: "songTitle.*artistName.*isrc"
---

<objective>
Scaffold the iOS Xcode project with MVVM folder structure, Swift data models mirroring backend types, and an API client skeleton.

Purpose: The iOS app is the primary user-facing interface. Setting up the project structure, data models, and networking skeleton now means Phase 6 (Core iOS App) can jump straight into building screens without any setup overhead.
Output: A buildable Xcode project with MVVM architecture, complete data models, and an API client ready for endpoint implementation.
</objective>

<execution_context>
@/Users/stefanpanaite/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefanpanaite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Models should mirror these backend shared types exactly -->

From packages/shared/src/types/detection.ts:
- DetectionEvent: id, stationId, detectedAt, songTitle, artistName, albumTitle?, isrc?, confidence, durationMs, rawCallbackId?, createdAt
- AirplayEvent: id, stationId, startedAt, endedAt, songTitle, artistName, isrc?, playCount, snippetUrl?, createdAt

From packages/shared/src/types/station.ts:
- Station: id, name, streamUrl, status, country, stationType, lastHeartbeat?, restartCount, createdAt, updatedAt

From packages/shared/src/types/user.ts:
- User: id, email, name, role (UserRole), scopeId?, isActive, lastLoginAt?, createdAt, updatedAt
- UserRole: ADMIN, ARTIST, LABEL, STATION

From packages/shared/src/types/snippet.ts:
- AudioSnippet: id, detectionId, stationId, storageKey, durationMs, encoding, bitrate, sizeBytes, presignedUrl?, createdAt

From packages/shared/src/types/invitation.ts:
- Invitation: id, code, role, scopeId?, status, createdById, redeemedById?, expiresAt, createdAt, redeemedAt?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with MVVM structure, data models, and API client</name>
  <files>
    apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj/project.pbxproj,
    apps/ios/myFuckingMusic/App/myFuckingMusicApp.swift,
    apps/ios/myFuckingMusic/App/ContentView.swift,
    apps/ios/myFuckingMusic/Models/Station.swift,
    apps/ios/myFuckingMusic/Models/Detection.swift,
    apps/ios/myFuckingMusic/Models/User.swift,
    apps/ios/myFuckingMusic/Models/AudioSnippet.swift,
    apps/ios/myFuckingMusic/Models/Invitation.swift,
    apps/ios/myFuckingMusic/Services/APIClient.swift,
    apps/ios/myFuckingMusic/Services/APIEndpoint.swift,
    apps/ios/myFuckingMusic/ViewModels/.gitkeep,
    apps/ios/myFuckingMusic/Views/.gitkeep,
    apps/ios/myFuckingMusic/Utilities/.gitkeep,
    apps/ios/myFuckingMusic/Resources/.gitkeep
  </files>
  <action>
Create the iOS project using the `xcodebuild` CLI or by creating the project structure manually with a valid `project.pbxproj`. The project MUST target iOS 17 minimum per locked decisions.

**Approach:** Use `swift package init` is NOT appropriate here -- this is a SwiftUI app, not a library. Instead, create the Xcode project via command line:

Option A (preferred): Run `xcodegen` if available, or create the project manually:
1. Create directory structure under `apps/ios/myFuckingMusic/`
2. Create all Swift source files
3. Generate the Xcode project using a `project.yml` spec for XcodeGen, OR create the project.pbxproj manually

Option B: If xcodegen is not available, create the Swift files first and then use `xcrun` commands, or provide instructions for the user to create the Xcode project wrapper. The Swift code is the critical output.

**MVVM Folder Structure:**
```
apps/ios/myFuckingMusic/
├── myFuckingMusic.xcodeproj/
├── App/
│   ├── myFuckingMusicApp.swift    # @main entry point
│   └── ContentView.swift          # Root view (placeholder)
├── Models/                         # Codable data models
├── ViewModels/                     # @Observable view models (empty for now)
├── Views/                          # SwiftUI views (empty for now)
├── Services/                       # API client, networking
├── Utilities/                      # Extensions, helpers (empty for now)
└── Resources/                      # Assets (empty for now)
```

**App entry point (`App/myFuckingMusicApp.swift`):**
```swift
import SwiftUI

@main
struct myFuckingMusicApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

**ContentView (`App/ContentView.swift`):**
Simple placeholder with app name text. Will be replaced in Phase 6.

**Data Models -- All Codable structs mirroring backend types:**

`Models/Station.swift`:
```swift
import Foundation

struct Station: Codable, Identifiable {
    let id: Int
    let name: String
    let streamUrl: String
    let stationType: String  // "radio" or "tv"
    let country: String
    let status: String       // active/inactive/error/restarting
    let lastHeartbeat: Date?
    let restartCount: Int
    let createdAt: Date
    let updatedAt: Date
}

enum StationType: String, Codable {
    case radio
    case tv
}

enum StreamStatus: String, Codable {
    case active, inactive, error, restarting
}
```

`Models/Detection.swift`:
```swift
import Foundation

struct Detection: Codable, Identifiable {
    let id: Int
    let stationId: Int
    let detectedAt: Date
    let songTitle: String
    let artistName: String
    let albumTitle: String?
    let isrc: String?
    let confidence: Double
    let durationMs: Int
    let rawCallbackId: String?
    let createdAt: Date
}

struct AirplayEvent: Codable, Identifiable {
    let id: Int
    let stationId: Int
    let startedAt: Date
    let endedAt: Date
    let songTitle: String
    let artistName: String
    let isrc: String?
    let playCount: Int
    let snippetUrl: String?
    let createdAt: Date
}
```

`Models/User.swift`:
```swift
import Foundation

struct User: Codable, Identifiable {
    let id: Int
    let email: String
    let name: String
    let role: UserRole
    let scopeId: Int?
    let isActive: Bool
    let lastLoginAt: Date?
    let createdAt: Date
    let updatedAt: Date
}

enum UserRole: String, Codable {
    case admin, artist, label, station
}
```

`Models/AudioSnippet.swift`:
```swift
import Foundation

struct AudioSnippet: Codable, Identifiable {
    let id: Int
    let detectionId: Int
    let stationId: Int
    let storageKey: String
    let durationMs: Int
    let encoding: String
    let bitrate: Int
    let sizeBytes: Int
    let presignedUrl: String?
    let createdAt: Date
}
```

`Models/Invitation.swift`:
```swift
import Foundation

struct Invitation: Codable, Identifiable {
    let id: Int
    let code: String
    let role: UserRole
    let scopeId: Int?
    let status: InvitationStatus
    let createdById: Int
    let redeemedById: Int?
    let expiresAt: Date
    let createdAt: Date
    let redeemedAt: Date?
}

enum InvitationStatus: String, Codable {
    case pending, redeemed, expired, revoked
}
```

**API Client (`Services/APIClient.swift`):**
Custom URLSession-based client with async/await (NO Alamofire per locked decisions).

```swift
import Foundation

actor APIClient {
    static let shared = APIClient()

    private let session: URLSession
    private let decoder: JSONDecoder
    private var baseURL: URL

    private init() {
        let config = URLSessionConfiguration.default
        config.timeoutIntervalForRequest = 30
        self.session = URLSession(configuration: config)

        let decoder = JSONDecoder()
        decoder.keyDecodingStrategy = .convertFromSnakeCase
        decoder.dateDecodingStrategy = .iso8601
        self.decoder = decoder

        // Default to localhost for development
        self.baseURL = URL(string: "http://localhost:3000/api/v1")!
    }

    func setBaseURL(_ url: URL) {
        self.baseURL = url
    }

    func request<T: Decodable>(_ endpoint: APIEndpoint) async throws -> T {
        let url = baseURL.appendingPathComponent(endpoint.path)
        var request = URLRequest(url: url)
        request.httpMethod = endpoint.method.rawValue

        if let body = endpoint.body {
            request.httpBody = body
            request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        }

        // Auth token will be added in Phase 5
        // if let token = TokenStorage.accessToken {
        //     request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        // }

        let (data, response) = try await session.data(for: request)

        guard let httpResponse = response as? HTTPURLResponse else {
            throw APIError.invalidResponse
        }

        guard (200...299).contains(httpResponse.statusCode) else {
            throw APIError.httpError(statusCode: httpResponse.statusCode, data: data)
        }

        return try decoder.decode(T.self, from: data)
    }
}

enum HTTPMethod: String {
    case GET, POST, PUT, PATCH, DELETE
}

enum APIError: Error, LocalizedError {
    case invalidResponse
    case httpError(statusCode: Int, data: Data)
    case decodingError(Error)

    var errorDescription: String? {
        switch self {
        case .invalidResponse:
            return "Invalid server response"
        case .httpError(let code, _):
            return "HTTP error: \(code)"
        case .decodingError(let error):
            return "Decoding error: \(error.localizedDescription)"
        }
    }
}
```

**API Endpoint (`Services/APIEndpoint.swift`):**
```swift
import Foundation

enum APIEndpoint {
    // Health
    case health

    // Detections (Phase 6)
    // case detections(page: Int, perPage: Int)
    // case detectionsByDateRange(from: Date, to: Date)

    var path: String {
        switch self {
        case .health:
            return "/health"
        }
    }

    var method: HTTPMethod {
        switch self {
        case .health:
            return .GET
        }
    }

    var body: Data? {
        switch self {
        case .health:
            return nil
        }
    }
}
```

Create `.gitkeep` files in empty directories (ViewModels, Views, Utilities, Resources) so the MVVM structure is preserved in git.

**CodingKeys note:** Use `keyDecodingStrategy = .convertFromSnakeCase` on JSONDecoder so Swift camelCase properties automatically map to backend snake_case JSON. Do NOT add manual CodingKeys enums.
  </action>
  <verify>
    <automated>ls -la /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic/Models/ && ls -la /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic/Services/ && ls -la /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic/App/</automated>
  </verify>
  <done>All Swift source files exist. Data models mirror backend shared types. API client uses URLSession with async/await. MVVM folder structure is established.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify iOS project builds in Xcode</name>
  <files>apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj/project.pbxproj</files>
  <action>
    User verifies that the iOS Xcode project opens and builds correctly.
    What was built: iOS Xcode project with MVVM structure, Swift data models mirroring all backend types, and a URLSession-based API client skeleton.
    How to verify:
    1. Open the Xcode project: `open apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj`
    2. Verify the project opens without errors in Xcode
    3. Select an iOS 17+ simulator (e.g., iPhone 15)
    4. Build the project (Cmd+B) -- should compile with zero errors
    5. Run the project (Cmd+R) -- should show the placeholder ContentView
    6. Check the file navigator shows the MVVM folder structure (App, Models, ViewModels, Views, Services, Utilities, Resources)
    Resume signal: Type "approved" if the project builds and runs, or describe any issues.
  </action>
  <verify>
    <automated>ls /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>User confirms Xcode project opens, builds for iOS 17 simulator without errors, and displays the placeholder ContentView.</done>
</task>

</tasks>

<verification>
1. Xcode project exists at `apps/ios/myFuckingMusic/myFuckingMusic.xcodeproj`
2. Project builds for iOS 17 simulator without errors
3. All 5 model files exist (Station, Detection, User, AudioSnippet, Invitation)
4. APIClient.swift uses URLSession with async/await
5. MVVM folder structure preserved in git via .gitkeep files
</verification>

<success_criteria>
- iOS project opens in Xcode and builds for iOS 17 simulator
- Swift data models contain all fields matching backend shared types
- API client skeleton is ready for endpoint implementation in Phase 6
- MVVM folder structure is established for all subsequent iOS development
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`
</output>
